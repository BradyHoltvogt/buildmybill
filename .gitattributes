# Keep line endings consistent so diffs stay clean across machines
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Tesseract's language model is binary — never let it go through eol conversion
*.traineddata binary
